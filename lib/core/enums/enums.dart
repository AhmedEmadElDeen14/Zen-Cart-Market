enum RequestStatus { init, loading, success, failure }

enum ScreenType {
  init,
  loading,
  brandsSuccess,
  brandsError,
  categorySuccess,
  categoryFailures,
  productsSuccess,
  productsFailures,
  changeNavBar,
  cartFailures,
  cartSuccess,
  wishlistSuccess,
  wishlistFailures,
}
